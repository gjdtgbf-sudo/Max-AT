# 📋 مرجع سريع لأوامر AT

## الأوامر الأساسية الأكثر استخدامًا

### ⚡ اختبار سريع
```bash
AT              # اختبار الاتصال
AT+CGMI         # اسم الشركة المصنعة
AT+CGMM         # موديل المودم
AT+CGSN         # رقم IMEI
AT+CPIN?        # حالة بطاقة SIM
AT+CSQ          # قوة الإشارة (0-31)
AT+CREG?        # حالة تسجيل الشبكة
AT+COPS?        # المشغل الحالي
```

---

## 📱 إرسال SMS (خطوتين فقط)
```bash
AT+CMGF=1                    # تفعيل وضع النص
AT+CMGS="0501234567"         # رقم المستقبل
> Hello from GSM!            # نص الرسالة
# اضغط Ctrl+Z
```

---

## 🌐 الاتصال بالإنترنت (3 خطوات)
```bash
AT+CGDCONT=1,"IP","internet"    # إعداد APN (استبدل internet باسم APN الصحيح)
AT+CGACT=1,1                    # تفعيل الاتصال
AT+CGPADDR=1                    # الحصول على IP
```

**أسماء APN الشائعة:**
- السعودية: `internet` (STC), `zain`, `web.sa` (Mobily)
- مصر: `internet.vodafone.net`, `mobinilweb`
- الإمارات: `etisalat`, `internet` (Du)

---

## 📞 إجراء مكالمة
```bash
ATD0501234567;    # إجراء مكالمة
ATA               # الرد على مكالمة واردة
ATH               # إنهاء المكالمة
```

---

## 🔍 تشخيص سريع

### فحص الاتصال والشبكة
```bash
AT                     # يجب أن يرد: OK
AT+CPIN?              # يجب أن يرد: +CPIN: READY
AT+CSQ                # قوة الإشارة (أكثر من 10 = جيد)
AT+CREG?              # يجب أن يرد: +CREG: 0,1 أو 0,5
AT+CGATT?             # يجب أن يرد: +CGATT: 1
```

### تفسير النتائج

#### قوة الإشارة (CSQ)
```
+CSQ: XX,99

XX = قوة الإشارة:
  0-9    = ضعيف جداً ❌
  10-14  = ضعيف ⚠️
  15-19  = جيد ✅
  20-31  = ممتاز 🌟
  99     = غير معروف
```

#### حالة التسجيل (CREG)
```
+CREG: 0,N

N = الحالة:
  0 = غير مسجل، لا يبحث
  1 = مسجل، شبكة محلية ✅
  2 = غير مسجل، يبحث...
  3 = تم الرفض ❌
  4 = غير معروف
  5 = مسجل، تجوال ✅
```

---

## 🛠️ الإعدادات الأساسية

### إعداد المودم من الصفر
```bash
ATZ              # إعادة تعيين
ATE0             # إيقاف echo
AT+CMGF=1        # وضع النص للـ SMS
AT+CNMI=2,1      # إشعارات SMS
AT+CLIP=1        # عرض رقم المتصل
AT+CFUN=1        # تفعيل كامل الوظائف
```

---

## 🔐 التعامل مع PIN

```bash
AT+CPIN?              # فحص حالة PIN
AT+CPIN="1234"        # إدخال PIN (استبدل 1234)
AT+CLCK="SC",0,"1234" # إلغاء قفل PIN
AT+CPWD="SC","1234","5678"  # تغيير PIN من 1234 إلى 5678
```

---

## 📊 قراءة الرسائل

```bash
AT+CMGL="ALL"         # جميع الرسائل
AT+CMGL="REC UNREAD"  # غير المقروءة فقط
AT+CMGR=1             # قراءة رسالة رقم 1
AT+CMGD=1             # حذف رسالة رقم 1
AT+CMGD=1,4           # حذف جميع الرسائل
```

---

## 🔧 أوامر الصيانة

### البحث عن الشبكات
```bash
AT+COPS=?    # البحث عن جميع الشبكات (يستغرق 30-120 ثانية)
AT+COPS=0    # تسجيل تلقائي
```

### إجبار شبكة معينة (السعودية)
```bash
AT+COPS=1,2,"42001",7    # Zain KSA LTE
AT+COPS=1,2,"42005",7    # STC (Al Jawal) LTE
AT+COPS=1,2,"42003",7    # Mobily LTE
```

### إعادة التشغيل
```bash
AT+CFUN=1,1    # إعادة تشغيل المودم
ATZ            # إعادة تعيين الإعدادات
```

---

## ⚡ Python - سكريبت سريع

```python
import serial
import time

# فتح الاتصال
ser = serial.Serial('/dev/ttyUSB0', 115200, timeout=1)

def at(cmd):
    ser.write((cmd + '\r\n').encode())
    time.sleep(0.5)
    return ser.read_all().decode()

# استخدام
print(at('AT'))
print(at('AT+CGSN'))  # IMEI
print(at('AT+CSQ'))   # Signal

# إرسال SMS
at('AT+CMGF=1')
at('AT+CMGS="0501234567"')
ser.write(b'Hello World')
time.sleep(0.5)
ser.write(bytes([26]))  # Ctrl+Z
time.sleep(2)
print(ser.read_all().decode())

ser.close()
```

---

## 🆘 حل سريع للمشاكل

| المشكلة | الحل السريع |
|---------|------------|
| لا يرد على AT | تحقق من المنفذ والـ Baud Rate (جرب 115200) |
| ERROR | تحقق من صحة الأمر أو دعم المودم له |
| CMS ERROR 304 | استخدم `AT+CMGF=1` قبل إرسال SMS |
| CMS ERROR 330 | تحقق من تركيب بطاقة SIM |
| +CME ERROR: SIM PIN | أدخل PIN: `AT+CPIN="1234"` |
| Network timeout | فحص الإشارة `AT+CSQ` وانتظر التسجيل |
| لا يتصل بالإنترنت | تأكد من APN الصحيح وأن `AT+CGATT?` يرد 1 |

---

## 📞 أرقام مراكز الخدمة (SMSC) - السعودية

```bash
# تحديد رقم SMSC يدوياً (إذا لزم الأمر)
AT+CSCA="+966500000100"   # STC
AT+CSCA="+966590000999"   # Zain
AT+CSCA="+966560010000"   # Mobily

# قراءة الرقم الحالي
AT+CSCA?
```

---

## 🌟 نصائح ذهبية

1. **دائماً ابدأ بـ `AT`** للتأكد من الاتصال
2. **استخدم `ATZ`** عند الحاجة لإعادة التعيين
3. **تحقق من `AT+CPIN?`** قبل أي عملية
4. **راقب `AT+CSQ`** - إذا كانت الإشارة ضعيفة، انتقل لمكان أفضل
5. **استخدم `ATE0`** لإيقاف echo والحصول على ردود نظيفة
6. **انتظر 2-5 ثواني** بعد تشغيل المودم قبل إرسال أوامر
7. **احفظ الإعدادات** باستخدام `AT&W` (إن كان مدعوماً)

---

## 📚 المصادر الكاملة

- [README.md](./README.md) - نظرة عامة على المشروع
- [USAGE_EXAMPLES.md](./USAGE_EXAMPLES.md) - أمثلة برمجية مفصلة
- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - دليل حل المشاكل الشامل
- **التطبيق التفاعلي**: [https://3000-i4sxv404utih0fhdfcp2g-0e616f0a.sandbox.novita.ai](https://3000-i4sxv404utih0fhdfcp2g-0e616f0a.sandbox.novita.ai)

---

**نسخة سريعة ومختصرة - للمزيد من التفاصيل، راجع الملفات الأخرى**
